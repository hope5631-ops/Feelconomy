version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    restart: always

  # Future: Add a database service like PostgreSQL if needed
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: todak_db
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
